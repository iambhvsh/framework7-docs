# Vercel Deployment

Make sure to install dependencies in the `worker` directory.
Deploy to Vercel using `vercel` inside `worker`. Ensure the environment variables `KV_REST_API_URL` and `KV_REST_API_TOKEN` are set.
`RATE_LIMIT_MAX` and `RATE_LIMIT_WINDOW_SECONDS` can also be configured.
